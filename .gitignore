node_modules/
*.log
.DS_Store
/tmp/
dist/
# Seed / app-export artifacts (op seed, op app export)
*.tar.gz
*.tgz
*.age
!**/fixtures/**/*.age
# Secrets — never commit
claude-token
*-token
.env
.env.*
# Platform state dirs booted in-repo (OP_ROOT=./…) — never commit
/new-op/
